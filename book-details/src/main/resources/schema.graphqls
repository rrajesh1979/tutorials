type Query {
    weatherBySol(id: ID): Weather
}

type Weather {
    id: ID,
    date: String,
    avgPressure: Float,
    roverPhoto: RoverPhoto
}

type RoverPhoto {
    id: ID
    img_src: String,
    name: String
}
